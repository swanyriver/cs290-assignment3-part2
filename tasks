Speed up search load,
currently because each are checked for duplicates it is nsquared

store them in an object with ids as name
then store array of ids in order
then construct array of gists from object


